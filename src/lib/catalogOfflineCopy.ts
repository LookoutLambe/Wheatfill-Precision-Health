/**
 * Shared copy for catalog pages when the API is unreachable and the UI shows built-in / fallback prices.
 * Keep wording aligned across hubs so patients and staff see one consistent story.
 */

/** Full-width banner on catalog / partner pages (class `orderNowOffline`). */
export const CATALOG_OFFLINE_BODY_CATALOG_PAGE =
  'Offline catalog: list prices below match our standard menu. Connect the live API to sync pricing, inventory, and checkout when your deployment is ready.'

/** Compact note on order summary (class `orderNowOffline--subtle`). */
export const CATALOG_OFFLINE_BODY_ORDER_SUMMARY =
  "You're viewing list prices for this catalog. Connect the live API for up-to-date inventory and checkout when your deployment is ready."

/** Accessible Hallandale table page (card body). */
export const CATALOG_OFFLINE_BODY_HALLANDALE_TABLE =
  'Showing the latest known Hallandale list prices. Connect the live API to sync live inventory and checkout.'
