/** Accessible name when the logo links home (matches header nav pattern). */
export const SITE_LOGO_ARIA_LABEL_HOME = 'Wheatfill Precision Health — Home'

/** Marketing slogan (text only — full logo stays in the site header). */
export const PRACTICE_SLOGAN = 'Precision Health. Elevated Living.'
